// mode_auto.h

#ifndef _MODE_AUTO_h
#define _MODE_AUTO_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif
void modeAuto(void);

#endif

